# Task Log: Publish ClawSeatbelt 0.1.0

## Goal

Ship the first public `clawseatbelt` package through the GitHub Actions release workflow.

## Verification

- Confirmed `clawseatbelt` is still unpublished on npm before tagging.
- Confirmed `NPM_TOKEN` exists in GitHub repository secrets.
- Ran `npm test`.
- Ran `npm pack --json --pack-destination .tmp/pack`.
- Ran `npm run verify:openclaw-lab`.
- Confirmed `package.json` and `openclaw.plugin.json` versions both resolve to `0.1.0`.

## Release Notes

- Publish path uses `.github/workflows/release.yml` and triggers on `v*` tags.
- Release artifact stays warning-free in a disposable OpenClaw install path.
- Package remains small and local-first, with benchmark-only harness code excluded from the published tarball.
